package oop

interface A{
    fun studentLogin()
    fun studentLogout()
}
//create a class called mca .ensure the mca can register and login.
//through inheritance ,ensure a governor can register and login henceforth implement
//allow the governor to display names of all students in the county on an array in the following maner:
//      *print the names with 5 or more characters
//      *ensure the names are printed in reverse
//      *display the count of names that were skipped on the array